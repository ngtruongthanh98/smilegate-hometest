{
  "name": "vite-vue-typescript-starter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add dist -f && git commit -m 'build: adding dist' && git subtree push --prefix dist origin gh-pages && git reset --hard HEAD~1"
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@types/node": "^17.0.25",
    "@vitejs/plugin-vue": "^2.3.1",
    "pathe": "^0.2.0",
    "sass": "^1.50.1",
    "typescript": "^4.5.4",
    "vite": "^2.9.5",
    "vue-tsc": "^0.34.7"
  }
}
